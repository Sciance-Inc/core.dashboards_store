---
title: Congés des employés
---

::alert{type=info}
Suit les congés de chaque employé pour chaque état associé pendant une période donnée. Le tableau de bord affiche le total de chaque état par lieu de travail, le total global d'un état précis et l'historique de chaque état.
::

# Déploiement
:badge[tag:emp_conge]{type="success"}
:badge[new in v0.7.0]

## Bases de données
La base de données `paie` doit être liée au projet. Consultez la section [lier une base de données](/fr/using/configuration/databases) pour plus d'informations.

## *Marts*
Les *marts* suivants doivent être activés pour que le tableau de bord fonctionne. Consultez la section [activer un *mart*](/fr/using/configuration/enabling) pour plus d'informations.

- `human_resources`

#### Spécification du projet *dbt*
> Mettez à jour votre fichier `cssXX.dashboards_store/dbt_project.yml` avec l'extrait suivant.

```yaml
# cssXX.dashboards_store/dbt_project.yml
models:
  core_dashboards_store:
    marts:
      human_resources:
        +enabled: True
    dashboards:
      human_resources:
        emp_conge:
          +enabled: true
    interfaces:
      paie:
        +enabled: true
```
